# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Folders

- `contract/` — OpenAPI 3.1 spec (`openapi.yaml`) and Redocly config. Source of truth for the HTTP API; consumed by `my-app/` codegen.
- `my-app/` — Spring Boot 4 / Java 25 backend (Gradle). See [my-app/CLAUDE.md](my-app/CLAUDE.md).
